package domain.model;

/**
 * Enumeration representing transaction types.
 * <p>
 * This enumeration defines two types of transactions: DEBIT (debit) and CREDIT (credit).
 * It is used to specify the type of each transaction.
 */
public enum TransactionType {
    DEBIT, CREDIT
}